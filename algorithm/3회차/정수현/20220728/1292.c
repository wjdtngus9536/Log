#include<math.h>

x;
main(a,b)
{
    for(scanf("%d%d",&a,&b); b/a;)
//    for(int i; i<3; i++)
        x += sqrt(2*a++) + .5;
    printf("%d",x);
//    printf("%f", 1 + .5);
}

/*
while B//A:
    x += sqrt( 2 * A )
    A += 1
print(x)
*/